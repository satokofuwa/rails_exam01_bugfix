<!-- エラー原因: string型と整数型を混ぜて値を出力するコードになっていた
# 修正の意図: きちんとコメントを出力することができるようにする>
<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @blog.content %>
</p>

<% unless @blog.comments.count.zero? %>
  <hr>

  <p>
    <strong>Comment:</strong>
 <%= number =  @blog.comments.count %>
 <%= number.to_s + "comments" %>
  </p>

  <p>
    <%= render @blog.comments %>
  </p>
<% end %>

<p>
  <%= render 'comments/form', blog: @blog %>
</p>

<%= link_to 'Edit', '/blogs/#{blog.id}/edit' %> |
<%= link_to 'Back', blogs_path %>
